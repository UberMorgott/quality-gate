<Project Sdk="Microsoft.NET.Sdk">

  <!-- Stands in for the game: selftest builds it into ..\lib, where Mod's HintPath expects it. -->
  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
  </PropertyGroup>

</Project>
